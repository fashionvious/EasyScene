# 剪辑测试用例（基于实际素材）

> **生成日期**: 2026-06-03
> **素材目录**: `backend/47d8edbd-5299-4917-be75-1f36396b156d/`
> **测试目标**: 使用项目真实素材验证 AI 剪辑 Agent 的完整工作流能力
> **适用模型**: 已接入 LangChain Agent 的 LLM (如 GPT-4o / Claude Opus)

---

## 素材清单

### 视频素材 (15 个)

| 文件名 | 用途推断 |
|--------|---------|
| 缉凶者6_1.mp4 | 第1集/第1场 |
| 缉凶者6_2.mp4 | 第1集/第2场 |
| 缉凶者6_3.mp4 | 第1集/第3场 |
| 缉凶者6_4.mp4 | 第1集/第4场 |
| 缉凶者6_5.mp4 ~ 缉凶者6_9.mp4 | 第1集/第5~9场 |
| 缉凶者6_10.mp4 ~ 缉凶者6_15.mp4 | 第1集/第10~15场 |

### 人物三视图 (4 个)

| 文件名 | 角色 |
|--------|------|
| 白昊天_three_view_.png | 白昊天 |
| 丁锋_three_view_.png | 丁锋 |
| 李强_three_view_.png | 李强 |
| 吴天_three_view_.png | 吴天 |

### 背景图 (4 个)

| 文件名 | 说明 |
|--------|------|
| 1_bgbg.png | 背景图1 |
| 2_bgbg.png | 背景图2 |
| 3_bgbg.png | 背景图3 |
| 4_bgbg.png | 背景图4 |

### 分镜图 (47 个)

命名规则: `缉凶者6_X_Y_ff.png`(近景) / `缉凶者6_X_Y_lf.png`(远景)

### BGM (4 个)

| 文件名 | 风格 |
|--------|------|
| 古风 唯美舒缓 安静温柔：适合古诗词朗诵、散文朗读、传统文化_爱给网_aigei_com.mp3 | 古风/唯美舒缓 |
| 花团团-Tiny Ripple Notes_爱给网_aigei_com.mp3 | 轻柔/治愈 |
| 六一儿童节・欢快童趣 BGM六一派对_爱给网_aigei_com.mp3 | 欢快/童趣 |
| 温暖的晨间新闻背景音乐 by 引乐原创-完整版.mp3 | 温暖/新闻 |

---

## 1. P0 — 原子功能测试（单工具直接调用）

### P0-01: 素材文件解析

| 字段 | 内容 |
|------|------|
| **测试场景** | 用户通过文件名引用视频素材，Agent 自动解析为完整路径 |
| **用户提示词** | "帮我把 缉凶者6_1.mp4 这个视频文件找出来，看看它在哪个目录" |
| **预期行为** | Agent 调用 `resolve_media("缉凶者6_1.mp4")`，返回完整绝对路径。 |

### P0-02: 素材列表浏览

| 字段 | 内容 |
|------|------|
| **测试场景** | 用户想查看所有可用素材 |
| **用户提示词** | "我现在有哪些视频和音频可以用？全部列出来看看" |
| **预期行为** | Agent 调用 `list_media()`，返回所有已索引的媒体文件列表，应包含 15 个 mp4、4 个 mp3、70 个 png。 |

### P0-03: 特效/转场搜索

| 字段 | 内容 |
|------|------|
| **测试场景** | 搜索悬疑风格转场效果 |
| **用户提示词** | "给我搜一下有没有'故障'或者'glitch'风格的转场效果，我要做悬疑片" |
| **预期行为** | Agent 调用 `execute_cli_script(script_name="asset_search", args={"query": "故障 glitch", "category": "transitions"})`。 |

### P0-04: TTS 语音生成

| 字段 | 内容 |
|------|------|
| **测试场景** | 将悬疑旁白文字转为语音 |
| **用户提示词** | "帮我把这段旁白转成语音：'夜幕降临，真相浮出水面'，用低沉男声" |
| **预期行为** | Agent 先调用 `asset_search(query="低沉 男声", category="tts_speakers")` 查找发音人，再调用 `execute_cli_script(script_name="universal_tts", args={"text": "夜幕降临，真相浮出水面", "speaker": "<speaker_id>"})`。 |

### P0-05: 草稿查看

| 字段 | 内容 |
|------|------|
| **测试场景** | 查看已有草稿列表 |
| **用户提示词** | "看看我之前创建了哪些剪辑工程？" |
| **预期行为** | Agent 调用 `execute_cli_script(script_name="draft_inspector", args={"action": "list", "limit": 20})`。 |

### P0-06: 简单视频导入并保存

| 字段 | 内容 |
|------|------|
| **测试场景** | 将单个视频导入时间线并保存草稿 |
| **用户提示词** | "把 缉凶者6_1.mp4 导入到新工程'P0-06: 简单视频导入并保存'里，保存草稿" |
| **预期行为** | Agent 先调用 `resolve_media("缉凶者6_1.mp4")` → 得到路径后通过 `execute_jyproject_code` 执行: `project = JyProject("P0-06: 简单视频导入并保存", overwrite=True); project.add_media_safe("<路径>"); project.save()`。 |

### P0-07: 添加基础文字字幕

| 字段 | 内容 |
|------|------|
| **测试场景** | 在视频上添加标题文字 |
| **用户提示词** | "在'P0-06: 简单视频导入并保存'这个工程里第 0 秒到第 3 秒之间，加上一行白色大字'缉凶者'，放在画面正中偏下" |
| **预期行为** | Agent 通过 `execute_jyproject_code` 生成代码: `project = JyProject("P0-06: 简单视频导入并保存"); project.add_text_simple("缉凶者", start_time="0s", duration="3s", ...); project.save()`。 |

### P0-08: 添加视频滤镜

| 字段 | 内容 |
|------|------|
| **测试场景** | 给悬疑视频加上冷色调滤镜 |
| **用户提示词** | "给 缉凶者6_1.mp4 这个视频加上一个冷色调电影感的滤镜效果，偏暗偏蓝的那种。工程名叫'P0-08: 添加视频滤镜'" |
| **预期行为** | Agent 先调用 `asset_search(query="冷色 电影 暗蓝", category="filters")` 找到滤镜名，再通过 `execute_jyproject_code` 应用。 |

### P0-09: 添加本地 BGM

| 字段 | 内容 |
|------|------|
| **测试场景** | 为视频添加本地背景音乐 |
| **用户提示词** | "把'古风 唯美舒缓'这个 mp3 作为背景音乐，铺在'P0-06: 简单视频导入并保存'工程下面" |
| **预期行为** | Agent 先调用 `resolve_media("古风 唯美舒缓")` 找到 mp3 路径，再通过 `execute_jyproject_code` 执行 `project.add_audio_safe("<mp3路径>", "0s", "<时长>", "BGM")`。 |

### P0-10: 导出视频

| 字段 | 内容 |
|------|------|
| **测试场景** | 将编辑好的草稿导出为 MP4 |
| **用户提示词** | "把'P0-06: 简单视频导入并保存'这个工程导出，要 1080P 30帧的 MP4，保存到桌面" |
| **预期行为** | Agent 调用 `execute_cli_script(script_name="auto_exporter", args={"draft_name": "P0-06: 简单视频导入并保存", "output_path": "<桌面路径>/P0-06: 简单视频导入并保存.mp4", "resolution": "1080", "framerate": "30"})`。 |

### P0-11: J-Cut 声音先行

| 字段 | 内容 |
|------|------|
| **测试场景** | 悬疑开场：BGM 比画面先出来 |
| **用户提示词** | "帮我把 缉凶者6_1.mp4 做一个悬疑开场：先用'古风 唯美舒缓'那个 mp3 提前 2 秒响起来，然后画面再出来，持续 8 秒。工程名叫'P0-11: J-Cut 声音先行'" |
| **预期行为** | Agent 先 `resolve_media` 解析两个文件路径，再调用 `apply_jcut(project_name="P0-11: J-Cut 声音先行", video_path="<缉凶者6_1.mp4路径>", audio_path="<古风mp3路径>", audio_lead_us=2_000_000, video_duration="8s")`。 |

### P0-12: 分屏对比

| 字段 | 内容 |
|------|------|
| **测试场景** | 左右分屏展示两个场景 |
| **用户提示词** | "左边放 缉凶者6_1.mp4 右边放 缉凶者6_2.mp4，同时播放 10 秒，做个左右对比的效果。工程名叫'P0-12: 分屏对比'" |
| **预期行为** | Agent 调用 `apply_split_screen(project_name="P0-12: 分屏对比", video_paths=["<缉凶者6_1.mp4路径>", "<缉凶者6_2.mp4路径>"], template="2H", duration="10s")`。 |

### P0-13: 双语字幕

| 字段 | 内容 |
|------|------|
| **测试场景** | 为悬疑片添加中英双语字幕 |
| **用户提示词** | "在'P0-06: 简单视频导入并保存'这个工程里，加一段中英双语字幕：0 到 3 秒显示'真相只有一个 / The truth is only one'，3 到 6 秒显示'嫌疑人就在我们中间 / The suspect is among us'" |
| **预期行为** | Agent 调用 `add_dual_subtitles(project_name="P0-06: 简单视频导入并保存", subtitle_pairs=[{"start_us":0, "duration_us":3_000_000, "primary_text":"真相只有一个", "secondary_text":"The truth is only one"}, {"start_us":3_000_000, "duration_us":3_000_000, "primary_text":"嫌疑人就在我们中间", "secondary_text":"The suspect is among us"}])`。 |

### P0-14: 缩放转场

| 字段 | 内容 |
|------|------|
| **测试场景** | 两个场景之间做缩放转场 |
| **用户提示词** | "把 缉凶者6_1.mp4 和 缉凶者6_2.mp4 接在一起，中间来一个放大的缩放转场过渡，过渡时间 0.3 秒。工程名叫'P0-14: 缩放转场'" |
| **预期行为** | Agent 调用 `apply_zoom_transition(project_name="P0-14: 缩放转场", video_path_1="<缉凶者6_1.mp4路径>", video_path_2="<缉凶者6_2.mp4路径>", zoom_peak=1.5, ramp_duration_us=300_000)`。 |

### P0-15: BGM 闪避

| 字段 | 内容 |
|------|------|
| **测试场景** | 人声旁白出现时自动压低背景音乐 |
| **用户提示词** | "我有个工程'P0-11: J-Cut 声音先行'，BGM 轨道叫'BGM'。第 2 到 5 秒和第 8 到 12 秒有人声旁白，帮我把这两段的 BGM 音量自动压低到 20%，淡入淡出 0.3 秒" |
| **预期行为** | Agent 调用 `apply_bgm_ducking(project_name="P0-11: J-Cut 声音先行", voice_segments=[{"start_us":2_000_000, "end_us":5_000_000}, {"start_us":8_000_000, "end_us":12_000_000}], bgm_track_name="BGM", duck_volume=0.2, fade_us=300_000)`。 |

### P0-16: 片段重排

| 字段 | 内容 |
|------|------|
| **测试场景** | 调整已有草稿中视频片段的播放顺序 |
| **用户提示词** | "我工程'P0-16: 片段重排'里有 4 个片段，把它们的顺序倒过来：原来 0,1,2,3 改成 3,2,1,0 的顺序播放" |
| **预期行为** | Agent 调用 `reorder_segments(project_name="P0-16: 片段重排", new_order=[3, 2, 1, 0])`。 |

---

## 2. P1 — 复合逻辑测试（多步编排）

### P1-01: 悬疑短片多场景拼接 + 转场 + BGM

| 字段 | 内容 |
|------|------|
| **测试场景** | 悬疑短剧混剪：导入多段素材、自动拼接、在片段间添加叠化转场、铺 BGM |
| **用户提示词** | "帮我把缉凶者6_1.mp4到缉凶者6_5.mp4这5段视频拼接成一个连播视频。片段之间用'叠化'转场过渡，最后铺上'古风 唯美舒缓'那个BGM。工程名叫'P1-01: 悬疑短片多场景拼接+转场+BGM'" |
| **预期行为** | Agent 通过 `execute_jyproject_code` 生成多步代码: ① 对 5 个片段分别调用 `add_media_safe` 依次导入 ② 在片段衔接处调用 `add_transition_simple("叠化", seg, "1s")` ③ 调用 `add_audio_safe` 导入古风BGM ④ `save()`。 |

### P1-02: J-Cut 开场 + 旁白配音 + 字幕

| 字段 | 内容 |
|------|------|
| **测试场景** | 悬疑短剧开场：声音先出来制造悬念，画面随后跟上，同步生成字幕 |
| **用户提示词** | "做一个悬疑短剧的开场：先用低沉旁白'夜幕降临，真相浮出水面'领先画面 2 秒播放，然后画面从 缉凶者6_1.mp4 开始播放，持续 8 秒。旁白要有中文字幕，放在画面底部。工程名叫'P1-02: J-Cut开场+旁白配音+字幕'" |
| **预期行为** | Agent 通过 `execute_jyproject_code`: ① `project.add_narrated_subtitles("夜幕降临，真相浮出水面", speaker="低沉男声", start_time="0s", track_name="NarrationSub")` ② `project.add_clip("缉凶者6_1.mp4", source_start="0s", duration="8s", target_start="2s")` ③ `save()`。 |

### P1-03: 分屏对比 + 人物三视图标注

| 字段 | 内容 |
|------|------|
| **测试场景** | 角色对比：左右分屏展示两个角色三视图 + 文字标注 |
| **用户提示词** | "做一个角色对比展示：左边放 白昊天_three_view_.png，右边放 丁锋_three_view_.png，两个画面同时播放 5 秒。然后在画面底部加上文字标注'左: 白昊天 | 右: 丁锋'。工程名叫'P1-03: 分屏对比+人物三视图标注'" |
| **预期行为** | Agent 优先选择 `apply_split_screen` 工具: ① `apply_split_screen(project_name="P1-03: 分屏对比+人物三视图标注", video_paths=["<白昊天_three_view_.png>", "<丁锋_three_view_.png>"], template="2H", duration="5s")` ② `add_text_simple(...)` 添加底部标注。 |

### P1-04: 滤镜 + 关键帧动画 + 音频淡入淡出

| 字段 | 内容 |
|------|------|
| **测试场景** | 视觉叙事：画面从放大状态逐渐缩小到正常，同时叠加冷色滤镜，音频淡入 |
| **用户提示词** | "导入 缉凶者6_3.mp4 整个片段。开始的时候画面放大到 1.5 倍，在半秒内缩回正常。同时加一个'青橙电影'滤镜，让音频在开头 1 秒内从无声渐变到正常。工程名叫'P1-04: 滤镜+关键帧动画+音频淡入淡出'" |
| **预期行为** | Agent 通过 `execute_jyproject_code`: ① `seg = project.add_media_safe("缉凶者6_3.mp4", "0s", "<时长>")` ② `seg.add_keyframe(KeyframeProperty.uniform_scale, 0, 1.5)` + `seg.add_keyframe(KeyframeProperty.uniform_scale, 500_000, 1.0)` ③ `project.add_effect_simple("青橙电影", "0s", "<时长>")` ④ `seg.add_fade("1s", "0s")`。 |

### P1-05: AI 粗剪 + 人工微调流水线

| 字段 | 内容 |
|------|------|
| **测试场景** | 先用 AI 分析视频自动生成粗剪草稿，再手动调整片段顺序 |
| **用户提示词** | "先用 AI 帮我分析一下 缉凶者6_1.mp4，找出所有精彩片段做个粗剪。然后我要看一下这个粗剪草稿里面有哪些片段，把前三个片段的顺序倒过来排列。工程名叫'P1-05: AI粗剪+人工微调流水线'" |
| **预期行为** | ① Agent 调用 `execute_cli_script(script_name="smart_rough_cut", args={"video": "缉凶者6_1.mp4"})` → 生成粗剪草稿 ② 调用 `execute_cli_script(script_name="draft_inspector", args={"action": "summary", "name": "<草稿名>"})` ③ 调用 `reorder_segments(project_name="<草稿名>", new_order=[2, 1, 0])`。 |

### P1-06: TTS 配音 + 字幕 + BGM 闪避三层混音

| 字段 | 内容 |
|------|------|
| **测试场景** | 悬疑纪录片：AI 旁白配音 + 时间对齐字幕 + BGM 自动闪避 |
| **用户提示词** | "我有一段视频 缉凶者6_2.mp4。帮我: 1) 用低沉男声为这段旁白配音'每一个细节都指向同一个嫌疑人' 2) 字幕要随着旁白逐句出现 3) 背景铺上'古风 唯美舒缓'BGM 4) 旁白出现时 BGM 自动压低到 20%。工程名叫'P1-06: TTS配音+字幕+BGM闪避三层混音'" |
| **预期行为** | Agent 通过 `execute_jyproject_code`: ① `project.add_narrated_subtitles("每一个细节都指向同一个嫌疑人", speaker="低沉男声", "0s", "NarrationSub")` ② `project.add_audio_safe("古风mp3路径", "0s", "<时长>", "BGM")` ③ `apply_bgm_ducking(project_name="...", voice_segments=[...], duck_volume=0.2)` ④ `save()`。 |

### P1-07: J-Cut + 分屏 + BGM 闪避组合

| 字段 | 内容 |
|------|------|
| **测试场景** | 悬疑片高级开场：J-Cut 悬念开场 → 分屏展示角色 → 旁白时 BGM 自动压低 |
| **用户提示词** | "帮我做一个悬疑片的 15 秒高级开场：1) 先用 J-Cut 效果，'古风 唯美舒缓'BGM 比画面提前 1 秒响起 2) 主画面用 缉凶者6_1.mp4，持续 8 秒 3) 同时分屏对比左边放 白昊天_three_view_.png、右边放 丁锋_three_view_.png 4) 第 2 到 6 秒有旁白'嫌疑人与受害者，谁在说谎'，这期间 BGM 压低到 25% 5) 工程名叫'P1-07: J-Cut+分屏+BGM闪避组合'" |
| **预期行为** | Agent 多步编排: ① `apply_jcut(...)` ② `apply_split_screen(...)` ③ `apply_bgm_ducking(...)` ④ 测试重点: Agent 能否正确处理多个 write 工具对同一草稿的操作顺序。 |

### P1-08: 缩放转场 + 逐字高亮字幕 + 音频变速

| 字段 | 内容 |
|------|------|
| **测试场景** | 悬疑宣传片：缩放转场连接两个场景 + KTV 逐字高亮字幕 + 结尾音频慢放 |
| **用户提示词** | "做一个 20 秒的悬疑宣传片：1) 缉凶者6_1.mp4 和 缉凶者6_2.mp4 用缩放转场连接 2) 配一段逐字高亮字幕'真相终将大白于天下'，从第 2 秒开始，每个字 0.3 秒 3) 最后 3 秒做个 0.5 倍速的慢放收尾。工程名叫'P1-08: 缩放转场+逐字高亮字幕+音频变速'" |
| **预期行为** | ① `apply_zoom_transition(project_name="P1-08: 缩放转场+逐字高亮字幕+音频变速", video_path_1="<缉凶者6_1.mp4>", video_path_2="<缉凶者6_2.mp4>", zoom_peak=1.5)` ② `add_karaoke_subtitle(project_name="P1-08: 缩放转场+逐字高亮字幕+音频变速", text="真相终将大白于天下", char_timestamps=[...])` ③ `apply_audio_speed(project_name="P1-08: 缩放转场+逐字高亮字幕+音频变速", track_name="AudioTrack", segment_index=0, speed=0.5)`。 |

### P1-09: 背景图 + 人物三视图画中画

| 字段 | 内容 |
|------|------|
| **测试场景** | 用背景图做底，叠加人物三视图做画中画展示 |
| **用户提示词** | "用 1_bgbg.png 做背景，在上面叠加 白昊天_three_view_.png 做画中画展示，人物图缩小到画面右下角，持续 5 秒。工程名叫'P1-09: 背景图+人物三视图画中画'" |
| **预期行为** | Agent 通过 `execute_jyproject_code`: ① `project.add_media_safe("1_bgbg.png", "0s", "5s")` ② `pip = project.add_media_safe("白昊天_three_view_.png", "0s", "5s", track_name="PIP")` ③ 通过 `ClipSettings(scale=0.3, position_x=0.35, position_y=-0.35)` 设置画中画位置和大小 ④ `save()`。 |

### P1-10: 多角色三视图轮播 + 转场

| 字段 | 内容 |
|------|------|
| **测试场景** | 四个角色三视图依次展示，之间加推拉转场 |
| **用户提示词** | "把 白昊天_three_view_.png、丁锋_three_view_.png、李强_three_view_.png、吴天_three_view_.png 这四张人物图依次展示，每张 3 秒，之间用从右往左的推拉转场过渡。工程名叫'P1-10: 多角色三视图轮播+转场'" |
| **预期行为** | Agent 通过 `execute_jyproject_code`: ① 依次 `add_media_safe` 导入 4 张图 ② 在衔接处调用 `add_transition_simple("推拉", seg, "0.5s")` 或使用 `apply_push_transition` ③ `save()`。 |

---

## 3. P2 — 边界与容错测试

### P2-01: 模糊参数推导

| 字段 | 内容 |
|------|------|
| **测试场景** | 用户给出模糊时间描述，考验 Agent 的参数归一化能力 |
| **用户提示词** | "帮我把 缉凶者6_1.mp4 的前三分之一裁剪出来，然后画面整体往左挪一点，加个毛玻璃效果的背景。工程名叫'P2-01: 模糊参数推导'" |
| **预期行为** | Agent 应: ① 用 `resolve_media` 找到视频 ② 获取总时长 ③ 计算 `source_start="0s", duration=f"{total_dur/3}s"` ④ `add_background_filling("blur", blur=0.375)` ⑤ "往左挪" 映射为 `ClipSettings(transform_x=-0.1)`。 |

### P2-02: 极端时间戳处理

| 字段 | 内容 |
|------|------|
| **测试场景** | 用户给出超出素材范围的时间 |
| **用户提示词** | "从 缉凶者6_1.mp4 的第 100 秒位置开始，裁剪 0.5 秒的画面，然后再从第 500 秒位置裁剪 3 秒。工程名叫'P2-02: 极端时间戳处理'" |
| **预期行为** | Agent 执行时检测到超出时长，向用户反馈"第 100 秒超出视频总时长"，不应崩溃或静默失败。 |

### P2-03: 不存在的资源引用

| 字段 | 内容 |
|------|------|
| **测试场景** | 用户引用了不存在的文件名或特效名 |
| **用户提示词** | "在 缉凶者6_1.mp4 上添加一个叫'七彩螺旋光晕'的特效，然后用 nonexistent_music.mp3 做背景音乐。工程名叫'P2-03: 不存在的资源引用'" |
| **预期行为** | ① `asset_search(query="七彩螺旋光晕")` 若无结果应告知用户 ② `resolve_media("nonexistent_music.mp3")` 应返回"文件未找到"，Agent 应提示用户而非继续执行。 |

### P2-04: 中英文混合特效名

| 字段 | 内容 |
|------|------|
| **测试场景** | 用户用英文名称描述中文软件中的特效 |
| **用户提示词** | "把缉凶者6_1.mp4和缉凶者6_2.mp4之间的转场改成 typewriter 风格，然后在视频上加一个 cyberpunk 滤镜。工程名叫'P2-04: 中英文混合特效名'" |
| **预期行为** | Agent 遇到模糊英文术语时应调用 `asset_search` 搜索，利用同义词映射尝试匹配，若无法精确匹配应提示用户选择。 |

### P2-05: 超长多操作编排

| 字段 | 内容 |
|------|------|
| **测试场景** | 用户在一条指令中包含大量操作步骤，考验 Agent 的任务分解能力 |
| **用户提示词** | "帮我做一条完整的悬疑短片：1) 导入 缉凶者6_1.mp4 开场 3 秒 2) 导入 缉凶者6_2.mp4 到 缉凶者6_6.mp4 依次排列，每个 4 秒 3) 每段之间加 0.5 秒的叠化转场 4) 全局铺'古风 唯美舒缓'BGM 5) 前三秒做 J-Cut 处理，让 BGM 先响 1.5 秒再出画面 6) 所有片段加冷色电影滤镜 7) 最后导出为缉凶者混剪_1080P_30fps.mp4。工程名叫'P2-05: 超长多操作编排'" |
| **预期行为** | Agent 应通过 `execute_jyproject_code` 生成完整的编排代码，包含循环处理 5 个片段、转场、J-Cut 偏移、滤镜叠加、BGM、导出。 |

### P2-06: 空的/无意义参数

| 字段 | 内容 |
|------|------|
| **测试场景** | 用户给出空字符串或明显错误的参数 |
| **用户提示词** | "新建一个工程叫''（空的），把 缉凶者6_1.mp4 放进去，字幕文字也写成空的就行" |
| **预期行为** | Agent 应提示"工程名不能为空"或自动生成默认名，不应崩溃。 |

### P2-07: 多语言字幕同步

| 字段 | 内容 |
|------|------|
| **测试场景** | 中、英、日三语字幕时间轴必须严格对齐 |
| **用户提示词** | "给 缉凶者6_1.mp4 配上中日英三语字幕。第一句 0-3s: '真相只有一个 / The truth is only one / 真実は一つだけ'，第二句 3-8s: '嫌疑人就在我们中间 / The suspect is among us / 容疑者は私たちの間にいる'。工程名叫'P2-07: 多语言字幕同步'" |
| **预期行为** | Agent 通过 `execute_jyproject_code` 创建三条独立字幕轨道，使用完全相同的 `start_time` 和 `duration`。注意 `add_dual_subtitles` 仅支持双轨，三语必须走手写三轨代码。 |

### P2-08: 错误恢复——代码执行失败后的自我修正

| 字段 | 内容 |
|------|------|
| **测试场景** | Agent 生成的 JyProject 代码有语法错误，应在验证后发现并修正 |
| **用户提示词** | "导入 缉凶者6_1.mp4 并加上一个叫'不存在的滤镜名字'的滤镜。工程名叫'P2-08: 错误恢复'" |
| **预期行为** | ① Agent 先生成代码 ② 先调用 `validate_jyproject_code` 验证 ③ 若执行后报错，Agent 应在 ReAct 循环中捕获错误，调整代码后重新执行。 |

### P2-09: Phase 2 注入工具的草稿不存在容错

| 字段 | 内容 |
|------|------|
| **测试场景** | 对不存在的草稿调用 Phase 2 注入工具 |
| **用户提示词** | "对'不存在的工程'里的 segment 加一个从左到右的遮罩转场" |
| **预期行为** | Agent 调用 `inject_mask_transition(project_name="不存在的工程", ...)` → 工具返回 `{"ok": False, "reason": "FileNotFoundError"}` → Agent 告知用户草稿不存在，而非死循环重试。 |

### P2-10: 同名素材区分

| 字段 | 内容 |
|------|------|
| **测试场景** | 素材目录中存在多个同名模式文件（如缉凶者6_1_4_ff.png 有 5 个变体），Agent 需正确区分 |
| **用户提示词** | "帮我把缉凶者6_1_4的所有分镜图找出来列一下" |
| **预期行为** | Agent 调用 `list_media()` 后过滤出所有以"缉凶者6_1_4"开头的文件，列出: 缉凶者6_1_4_ff.png, 缉凶者6_1_4_ff_2.png, 缉凶者6_1_4_ff_3.png, 缉凶者6_1_4_ff_4.png, 缉凶者6_1_4_ff_5.png, 缉凶者6_1_4_lf.png。 |

### P2-11: BGM 风格匹配

| 字段 | 内容 |
|------|------|
| **测试场景** | 用户用自然语言描述 BGM 风格，Agent 需匹配到正确的本地 mp3 |
| **用户提示词** | "给缉凶者6_1.mp4配一个适合悬疑氛围的背景音乐。工程名叫'P2-11: BGM风格匹配'" |
| **预期行为** | Agent 应从 4 个本地 mp3 中选择"古风 唯美舒缓"作为最匹配悬疑氛围的 BGM，或调用 `add_cloud_music("悬疑")` 搜索云端。 |

---

## 4. 测试执行建议

### 4.1 优先级分布

| 级别 | 数量 | 覆盖工具 | 建议执行频率 | 预计单次耗时 |
|------|------|---------|-------------|------------|
| P0 (原子功能) | 16 | 基础 10 + Phase 1/2 独立工具 6 | 每次代码提交 | ~8 分钟 |
| P1 (复合逻辑) | 10 | 多工具编排 + 新旧工具混合 + 素材特性 | 每日回归 | ~25 分钟 |
| P2 (边界容错) | 11 | 模糊参数/极端时间/不存在资源/素材区分/BGM匹配 | 每周全量 | ~22 分钟 |

### 4.2 测试环境要求

- Agent 已加载 jianying-editor skill 的完整上下文
- 22 个工具全部注册到 Agent
- 素材目录 `47d8edbd-5299-4917-be75-1f36396b156d/` 已正确挂载，包含 15 个 mp4 + 4 个 mp3 + 70 个 png
- 剪映客户端已安装（导出类测试需要）
- LangFuse Trace 已启用（用于验证 Tool Calling 调用链）

### 4.3 验收通过标准

- P0 用例 **100%** 通过：每个提示词触发正确的 Tool 调用、提取正确的参数
- P1 用例 **≥80%** 通过（10 条中 ≥8 条正确完成多步编排）
- P2 用例 **≥73%** 通过（11 条中 ≥8 条正确兜底处理）
