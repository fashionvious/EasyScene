# EasyScene · AI 驱动的端到端视频生成平台

**一句话概述**：面向短剧/动画批量生产场景，用户输入剧本即可全自动完成角色设计、分镜策划、图片/视频生成与剪映精剪的全链路 AI 视频创作平台。

---

### 背景与目标

传统视频制作需在多个 AI 工具间手动切换，流程碎片化、效率低。本项目核心目标是：让 LLM 不仅能"生成内容"，还能直接**操控剪映客户端完成编辑**，实现从创意到成片的端到端闭环。我主导设计了剪映桌面端自动化编辑 Agent 及配套的任务编排与实时进度体系。

---

### 个人职责与技术要点

**剪映编辑 Agent（核心）**

- 基于 LangChain Agent 框架构建编辑 Agent，将剪映的素材导入、时间线编排、字幕/特效/转场、导出等能力封装为 **20+ 个声明式 Tool**，按执行模式、并发安全性、重试策略多维度标注，注册到统一注册表供编排器和 Agent 共享
- 设计**三级渐进式 Skill 披露机制**控制上下文窗口消耗：路由摘要常驻系统提示（<500 tokens）→ 类别详情按需注入 → 完整文档通过 Tool 按需加载；配合 token 预算实时计算与上下文自动压缩（使用量超 80% 触发），在窗口内高效调度 148 个 API 和 17 条编辑规则
- LLM 输出的声明式分镜方案（14 种编辑动作）经校验/编译两步转换为可执行编辑计划，自动生成剪映 Python 调用代码（含素材导入、TTS 旁白、特效、滤镜、转场、导出等）
- **容错体系**：针对 uiautomation/FFmpeg 等不稳定上游实现断路器保护（3 次连续失败熔断 + 恢复探测）；建立异常分类→重试策略映射（用户输入错误通知、基础设施自动退避重试、数据错误终止）

**编辑任务编排与实时进度**

- 设计**分步编排器**，支持串行/并发混合执行——自动识别无依赖相邻步骤并发执行，对 FFmpeg 等资源密集型操作独立限流，异步重量步骤委托 Celery 队列并轮询等待
- 每步执行后自动写数据库 checkpoint，支持暂停/恢复/重试/跳过单步；恢复时从 checkpoint 重建计划，跳过已完成步骤继续执行
- 基于 Redis PubSub 实现编辑进度实时推送，前端通过 WebSocket 订阅；Redis 不可用时自动降级为数据库轮询

**LangGraph 视频生成流水线**

- 设计包含角色提取、四视图生成、分镜生成、场景图、视频生成的多节点有状态工作流，自定义 Reducer 解决并行分支的并发状态冲突，基于中断/恢复机制实现人工审核（HITL）与断点续跑
- 设计基于 SHA-256 哈希的**确定性 Seed 派生方案**，按角色/场景/分镜/视频维度的命名空间派生，解决多阶段生成中人物长相与场景风格漂移的核心难题

**工程化与可观测性**

- FastAPI + React 19 + PostgreSQL + Redis + Celery + Docker Compose 全栈架构，Provider-Adapter-Task 三层模式适配 OpenAI/阿里通义/火山引擎三种模型服务
- 集成 Sentry + Langfuse 双通道可观测性，HTTP 日志自动脱敏，Mypy strict + Ruff + Pytest + Playwright E2E

### 技术栈

`Python` `FastAPI` `LangChain` `LangGraph` `Celery` `Redis` `PostgreSQL` `Docker` `React` `TypeScript` `Playwright` `uiautomation` `Sentry` `Langfuse`

### 项目成果

- 交付了覆盖"剧本→成片"全链路 + 剪映 Agent 自动化编辑的生产级 AI 视频生成平台
- 编辑 Agent 支持 20+ Tool、14 种编辑动作、自动代码生成、步骤级编排控制与实时进度推送
- **【量化占位：累计生成视频 XX 个，编辑 Agent 自动化成功率 XX%】**
- **【量化占位：相比人工剪辑，单视频制作时间从 XX 降至 XX，效率提升 XX%】**
